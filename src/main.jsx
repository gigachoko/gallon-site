import React from "react";
import ReactDOM from "react-dom/client";
import GallonSite from "./GallonSite.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GallonSite />
  </React.StrictMode>
);
